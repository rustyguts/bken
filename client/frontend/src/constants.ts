// Centralised constants — magic strings, protocol prefixes, and localStorage keys.

/** The custom protocol prefix used for bken invite links. */
export const BKEN_SCHEME = 'bken://'

// --- localStorage / config keys ---

/** localStorage key for the last server address the user connected to. */
export const LAST_CONNECTED_ADDR_KEY = 'bken:last-connected-addr'

/** localStorage key for the currently-applied DaisyUI theme name. */
export const THEME_STORAGE_KEY = 'bken-theme'
